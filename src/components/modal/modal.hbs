<div class="modal">
    <form class="modal__container">
        <h1>{{title}}</h1>
        {{#if isFile}}
            {{{inputFile}}}
        {{else}}
            {{{login}}}
        {{/if}}
        {{{submitButton}}}
    </form>
</div>
