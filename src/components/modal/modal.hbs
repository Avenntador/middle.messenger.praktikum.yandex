<div class="modal">
    <div class="modal__container">
        <h1>{{title}}</h1>
        {{#if type}}
            {{{login}}}
        {{else}}
            {{{inputFile}}}
        {{/if}}
        {{{submitButton}}}
    </div>
</div>